#Creating a Set from a String
#Goal: Quickly find all unique characters in a text string for analysis (e.g., character-level encoding).
#Task: Convert the string text = "machine_learning_pipeline" into a set of unique characters.

text = "machine_learning_pipeline"

new_set = set(text)
print(new_set)